<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="category">
	<insert id="insert" parameterType="categoryDTO">
		<![CDATA[
		    INSERT t_category(no, name, parent_no, member_no)
		    VALUES
		    (null, #{name}, #{parent_no}, #{member_no})
        ]]>

		<selectKey keyProperty="no" resultType="long" order="AFTER">
			<![CDATA[
			   select last_insert_id()
		    ]]>
		</selectKey>
	</insert>

	<select id="categoryList" parameterType="String" resultType="categoryDTO">
		<![CDATA[
		    SELECT
		       no, name, parent_no, member_no
		    FROM t_category
		    WHERE member_no = #{memberNo}
        ]]>
	</select>

</mapper>
